# {{ server_name }} MCP Server

This is a Model Context Protocol (MCP) server generated by `mcp_builder`.
It connects Cursor to the `{{ base_url }}` API.

## Setup

1.  **Environment Variables**:
    Copy `.env.example` to `.env` and fill in the required values.

    ```bash
    cp .env.example .env
    ```

2.  **Dependencies**:
    Ensure `httpx` and `mcp` are installed in your project environment.

    ```bash
    pip install httpx mcp
    ```

## Usage

This server is automatically registered in `mcp.json` at the repository root.
Restart Cursor or reload the window to activate it.

## Configuration

- **Base URL**: {{ base_url }}
- **Auth Type**: {{ auth_type }}





